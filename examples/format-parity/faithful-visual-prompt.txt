Transcribe the entire page as faithful Markdown from top to bottom. First scan the whole page, including small text at the edges and bottom. Preserve the original language, reading order, hierarchy, and every readable word, number, unit, table row, chart point, formula, label, status, button, caption, and footer.

For every chart, explicitly list every visible category and every visible data value in order, including intermediate points. For every workflow or diagram, list every visible node in its exact order and describe each visible relationship. Do not replace visible values or labels with a summary.

Use headings, paragraphs, lists, Markdown tables, and LaTeX as appropriate. Put a visible formula inside `$$...$$`. Write chart points as `Q1: 98%`, `Q2: 103%`, and so on. Write a visible ordered workflow as one exact line such as `Import -> Understand -> Search -> Act` before any visual description. Describe every non-text visual only as a blockquote beginning with "> [Visual]", including its important labels and relationships. Do not use Markdown image syntax. Do not summarize, translate, infer, invent, add columns, add categories, add statuses, add icons, or omit readable information. Mark unreadable content as "不可读" on Chinese pages or "unreadable" otherwise. Return only Markdown without metadata, commentary, or enclosing code fences.
